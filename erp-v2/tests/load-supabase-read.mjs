import {readFileSync} from 'node:fs';
const config=readFileSync(new URL('../config.js',import.meta.url),'utf8'),url=config.match(/supabaseUrl:\s*['"]([^'"]+)/)?.[1],key=config.match(/supabaseAnonKey:\s*['"]([^'"]+)/)?.[1];
if(!url||!key)throw Error('config.js 缺少 Supabase URL 或 anon key');
const total=Number(process.argv[2]||1000),concurrency=Number(process.argv[3]||50),target=`${url}/rest/v1/sites?select=id&limit=1`,latencies=[];let next=0,ok=0,failed=0;const errors=new Map(),started=performance.now();
async function worker(){while(true){const index=next++;if(index>=total)return;const begin=performance.now(),controller=new AbortController(),timer=setTimeout(()=>controller.abort(),10000);try{const response=await fetch(target,{signal:controller.signal,headers:{apikey:key,authorization:`Bearer ${key}`,'x-load-test-id':String(index)}});await response.arrayBuffer();if(response.ok)ok++;else{failed++;errors.set(String(response.status),(errors.get(String(response.status))||0)+1)}}catch(error){failed++;errors.set(error.name,(errors.get(error.name)||0)+1)}finally{clearTimeout(timer);latencies.push(performance.now()-begin)}}}
await Promise.all(Array.from({length:concurrency},worker));latencies.sort((a,b)=>a-b);const percentile=p=>latencies[Math.min(latencies.length-1,Math.floor(latencies.length*p))]||0,duration=(performance.now()-started)/1000;
console.log(JSON.stringify({target:'Supabase REST sites limit=1',total,concurrency,ok,failed,successRate:`${(ok/total*100).toFixed(2)}%`,durationSeconds:Number(duration.toFixed(2)),requestsPerSecond:Number((total/duration).toFixed(2)),latencyMs:{p50:Number(percentile(.5).toFixed(1)),p95:Number(percentile(.95).toFixed(1)),p99:Number(percentile(.99).toFixed(1)),max:Number((latencies.at(-1)||0).toFixed(1))},errors:Object.fromEntries(errors)},null,2));
